{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cloud list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Credentials</th>
                <th>Src</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cloud_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.credentials }}</td>
                <td>{{ entity.src }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cloud_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cloud_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cloud_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
